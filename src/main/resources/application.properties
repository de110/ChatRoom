
# # MySQL 설정
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# # DB Source URL
# spring.datasource.url=jdbc:mysql://localhost:3306/chat?allowPublicKeyRetrieval=TRUE&useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
# # DB username
# spring.datasource.username=sky
# # DB password
# spring.datasource.password=coof1206
# # true 설정시 JPA 쿼리문 확인 가능
spring.jpa.show-sql=true
# # DDL(create, alter, drop) 정의시 DB의 고유 기능을 사용할 수 있다.
spring.jpa.hibernate.ddl-auto=update
# # JPA의 구현체인 Hibernate가 동작하면서 발생한 SQL의 가독성을 높여준다.
spring.jpa.properties.hibernate.format_sql=true

# #flyway 오류 처리
spring.flyway.baseline-on-migrate = true

spring.flyway.schemas:EmployeeHistory

logging.level.org.hibernate.SQL=DEBUG
# # spring.jwt.secret=jwtsecretkey

# spring.datasource.hikari.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.hikari.jdbc-url=jdbc:mysql://localhost:3306/chat?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false
# spring.datasource.hikari.username=sky
# spring.datasource.hikari.password=coof1206
# spring.datasource.hikari.connection-timeout=3000
# spring.datasource.hikari.validation-timeout=3000
# spring.datasource.hikari.minimum-idle=5
# spring.datasource.hikari.max-lifetime=240000
# spring.datasource.hikari.maximum-pool-size=20
# spring.datasource.hikari.connection-test-query=SELECT NOW() FROM dual

# mybatis.config=mybatis-config.xml
# mybatis.type-aliases-package:com.example.chat1
# mybatis.mapper-locations:mybatis/mappers/*.xml

# database
spring.datasource.url = jdbc:mysql://localhost:3306/chat?characterEncoding=utf8
spring.datasource.username= root
spring.datasource.password= coof1206
spring.datasource.driver-class-name= com.mysql.cj.jdbc.Driver

# mybatis
mybatis.config=mybatis-config.xml
mybatis.type-aliases-package= com.example.chat2
mybatis.mapper-locations= mybatis/mappers/*.xml

# logging.level.org.hibernate.SQL=DEBUG
spring.jwt.secret=jwtsecretkey